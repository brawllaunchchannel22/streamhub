name: Publish Snap

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Electron app
        run: npm run build -- --publish never
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Snap
        uses: snapcore/action-build@v1
        id: build

      - name: Publish Snap via Snapcraft CLI
        env:
          SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.SNAPCRAFT_STORE_CREDENTIALS }}
          SNAP_FILE: ${{ steps.build.outputs.snap }}
        run: |
          mkdir -p ~/.config/snapcraft
          python3 -c "
          import os, json, base64

          raw = os.environ['SNAPCRAFT_STORE_CREDENTIALS']
          try:
              macaroon = json.loads(base64.b64decode(raw))['v']
          except Exception:
              macaroon = raw

          with open(os.path.expanduser('~/.config/snapcraft/credentials.txt'), 'w') as f:
              f.write(macaroon)
          "
          snapcraft upload --release=stable "$SNAP_FILE"
