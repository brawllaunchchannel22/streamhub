name: Publish Snap

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Electron app
        run: npm run build -- --publish never
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Snap
        uses: snapcore/action-build@v1
        id: build

      - name: Install Snapcraft CLI
        run: sudo snap install snapcraft --classic

      - name: Publish Snap to Store
        env:
          SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.SNAPCRAFT_STORE_CREDENTIALS }}
        run: |
          mkdir -p ~/.config/snapcraft
          echo "$SNAPCRAFT_STORE_CREDENTIALS" > ~/.config/snapcraft/credentials.txt
          snapcraft login --with ~/.config/snapcraft/credentials.txt || snapcraft login --with <(echo "$SNAPCRAFT_STORE_CREDENTIALS")
          snapcraft upload --release=stable ${{ steps.build.outputs.snap }}
